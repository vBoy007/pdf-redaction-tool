# КРАТКО РЪКОВОДСТВО ЗА СТАРТИРАНЕ

## 📦 Стъпка 1: Инсталация

```bash
cd pdf-redaction-app
npm install
```

⏱️ **Време**: ~2-3 минути (зависи от интернет връзката)

## 🚀 Стъпка 2: Стартиране

```bash
npm run dev
```

След това отворете браузър на адреса, който Vite покаже (обикновено http://localhost:5173)

## ✅ Стъпка 3: Тест

1. **Качете тестов PDF** (може да използвате който и да е PDF с подпис)
2. **Изберете "Заличаване"** (Square icon в toolbar)
3. **Тегли правоъгълник** върху подписа (click + drag)
4. **Експортирайте** в Secure Mode
5. **Проверете** новия файл - подписът трябва да е черен box

## 🎯 КРИТИЧНО: TRUE Redaction Test

След експорт на PDF:
1. Отворете файла в PDF viewer
2. Опитайте да копирате текста под заличеното - **трябва да е празно**
3. Отворете в Adobe Acrobat/Preview - **няма скрити слоеве**
4. Заличеното е **необратимо**

## 🐛 Ако има проблеми

### Грешка "Cannot find module"
```bash
rm -rf node_modules package-lock.json
npm install
```

### Canvas rendering проблеми
- Опреснете страницата (F5)
- Проверете конзолата за грешки
- Опитайте с по-малък PDF файл

### Export не работи
- Уверете се, че има поне 1 заличаване
- Проверете browser конзолата
- Опитайте Quick Mode вместо Secure Mode

## 📝 Структура на проекта

```
pdf-redaction-app/
├── src/
│   ├── components/      # React компоненти
│   │   ├── FileUpload.tsx
│   │   ├── Toolbar.tsx
│   │   ├── PDFViewer.tsx
│   │   ├── Sidebar.tsx
│   │   └── ExportModal.tsx
│   ├── lib/            # Core логика
│   │   ├── pdfEngine.ts       # PDF.js rendering
│   │   ├── redactionEngine.ts # TRUE redaction
│   │   └── exportEngine.ts    # Export to PDF
│   ├── stores/         # State management
│   │   └── useDocumentStore.ts
│   ├── types/          # TypeScript types
│   │   └── index.ts
│   ├── App.tsx         # Main app
│   ├── main.tsx        # Entry point
│   └── index.css       # Styles
├── package.json
├── vite.config.ts
├── tsconfig.json
└── README.md
```

## 💡 Бързи съвети

- **За най-добро качество**: Използвайте Secure Mode
- **За бързина**: Използвайте Quick Mode (но данните може да не са напълно заличени)
- **За големи файлове**: Разделете на по-малки части
- **За mobile**: Desktop версията е оптимизирана, mobile работи но е по-ограничена

## 🔥 Production Deployment

```bash
npm run build
```

Това генерира `dist/` директория, която може да хоствате на:
- Netlify
- Vercel
- GitHub Pages
- Cloudflare Pages
- Всеки static hosting

**Важно**: Всичко работи client-side, не е нужен бекенд сървър!

---

Въпроси? Проверете README.md за пълна документация.
