# 🔄 GIT UPDATE ИНСТРУКЦИИ

## ✅ FIX: Click Outside затваря панелите

### Какво е променено:
- Добавен **click outside detection**
- Панелите се затварят автоматично при click извън тях
- Само ЕДИН панел наведнъж (mutual exclusive)

---

## 📦 Как да update-неш GitHub и Netlify:

### Вариант 1: Бърз (Drag & Drop)
```
1. Download новия ZIP (pdf-redaction-app.zip)
2. Unzip го локално
3. Delete съдържанието на GitHub repo (освен .git папката)
4. Copy новите файлове
5. Git commit & push
```

### Вариант 2: Git команди (Terminal)

```bash
# 1. Unzip новия архив
unzip pdf-redaction-app.zip

# 2. Влез в твоето repo
cd път/до/твоето/repo

# 3. Изтрий старите файлове (освен .git)
rm -rf src public node_modules dist # и т.н.

# 4. Copy новите файлове
cp -r път/до/pdf-redaction-app/* .

# 5. Git status (виж какво е променено)
git status

# 6. Add всички промени
git add .

# 7. Commit с описание
git commit -m "Fix: Click outside затваря панелите автоматично"

# 8. Push към GitHub
git push origin main
```

---

## 🚀 Auto Deploy

След `git push`:
1. GitHub получава промените
2. Netlify auto-detect промените
3. Започва build (2-3 мин)
4. Deploy на live сайта

Можеш да гледаш progress в Netlify dashboard → Deploys

---

## ✅ Тествай след deploy:

1. Open сайта
2. Upload PDF
3. Click на redaction box → panel се отваря
4. Click навсякъде извън → **panel се затваря** ✅
5. Click на text → panel се отваря
6. Click извън → **panel се затваря** ✅
7. Click на redaction, после на text → **само text panel е отворен** ✅

---

## 🐛 Ако има проблем:

Netlify Deploys → виж build log

Ако има грешка, изпрати ми я!

---

## 💡 Pro Tip:

За следващи промени:
```bash
# След промени в кода:
git add .
git commit -m "Описание на промяната"
git push

# Netlify auto-deploy! 🚀
```

**Готово! Push и чакай deploy!** ⏱️
